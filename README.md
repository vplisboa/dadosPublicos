# dadosPublicos

## Como Rodar:
A partir do diretório raiz do projeto:

    $ docker build -t parlamentares .
    
    $ docker run -p 8080:8080 -it parlamentares 
    
 
 Agora para acessar a aplicação basta abrir o navegador e acessar: http://localhost:8080    